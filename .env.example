# Production frontend URL for CORS (no trailing slash).
# FRONTEND_URL=https://www.sincer.info

# Optional: additional comma-separated allowed origins (preview URLs, Vercel, etc.).
# FRONTEND_URLS=https://stock-flash-test-frontend.vercel.app
