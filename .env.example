# Production frontend URL for CORS (Vercel deployment URL, no trailing slash).
# FRONTEND_URL=https://your-app.vercel.app

# Optional: additional comma-separated allowed origins (preview URLs, custom domains).
# FRONTEND_URLS=https://your-app-git-main.vercel.app,https://www.yourdomain.com
